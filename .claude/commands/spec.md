---
description: "Generate GitHub issues from an approved spec, add them to Project #2, auto-assign per repo, and apply Phase 4 labels."
---

# /spec — Issue generator from product specs

You are a project manager assistant for 321Fit. Your job is to break down approved product specs (from `project-spec/specs/<module>.md`) into actionable GitHub issues across the relevant repos, add them to Project #2, and apply consistent labels and assignees.

Argument: `$ARGUMENTS` — the spec file name without extension (e.g. `dashboard`, `group-training`, `location-picker`).

---

## Pre-flight inputs (read in parallel via the Agent tool)

**Required:**
- `project-spec/specs/<spec-name>.md` — the spec being broken down (source of truth). **Pay special attention to the bottom-of-file "Related specs / references" section** — every link in it must be propagated into each generated issue's "Related specs" block, so dev agents do not miss cross-spec dependencies (e.g. dashboard depends on onboarding-wizard step semantics, review-queue data source, event-statuses enum)
- `project-spec/SPEC_TEMPLATE.md` — confirms section semantics
- `project-spec/CLAUDE.md` — repo and Project #2 conventions
- `project-spec/contracts/_baseline.openapi.yaml` — backend baseline (for backend issues that touch endpoints)
- `project-spec/contracts/<spec-name>.openapi.yaml` — module contract fragment if it exists (otherwise note "to be generated by /architect impl-doc")

**Reference memory:**
- `reference_team_assignees` — per-repo default GitHub usernames for assignment
- `project_phase4_plan` — module ordering and execution philosophy
- `project_phase4_specs_audit` — which specs are clean vs have new endpoints expected
- `feedback_task_creation` — issue body must contain endpoints, layout code, error recovery, deps, links
- `feedback_keep_existing_endpoints` — never propose endpoint renames

**Repo CLAUDE.md files** (read the ones the spec touches):
- `poly-backend/CLAUDE.md`
- `321fit_ios/CLAUDE.md`
- `321fit_android_new/CLAUDE.md`
- `voice_control/CLAUDE.md` (skip — Phase 4 defers voice)

---

## Step 1 — Classify the spec

Determine the spec's character based on its content:

| Type | Signal | Pattern |
|---|---|---|
| **module** | Spec describes a single screen / module touched by 1-3 platforms with parallel work | One issue per platform (`<module>` × `repo:poly-backend` / `repo:321fit_ios` / `repo:321fit_android_new`) |
| **cross-cutting / flow** | Spec describes a user journey or feature spanning multiple modules across platforms | **Epic** issue in `project-spec` + sub-tasks in each touching platform repo |
| **reusable component** | Spec is for a shared component picker / atomic UI used by multiple parent specs | One issue per platform consuming the component (or batch into design-tokens if the component itself is missing) |
| **infrastructure** | Spec is for assets, contracts, build pipeline | Single issue in `design-tokens` or `project-spec` |

Heuristic — read Implementation header in the spec:
- Lists iOS + Backend + Android implementation rows → **module** or **cross-cutting**
- Distinguish further: if Spec section "User stories" describes a multi-step journey, it's **cross-cutting**; if it's "screen behavior", it's **module**

If unsure, **ask the user** before proceeding to issue generation. Do not guess.

---

## Step 2 — Plan issues

Build a planned issue list. Each entry has:
- Repo
- Title (Conventional Commits style)
- Labels (must include `phase-4` + `module:<spec-name>` + `repo:<repo>`; add `epic` / `pilot` / `type:*` as relevant)
- Assignees (from `reference_team_assignees`)
- Body (full template, see Step 3)
- Project: #2

For **module specs** (dashboard, payments, etc.), produce:
- 1 issue per platform repo (backend, iOS, Android — skip platforms not touched by the spec)
- Each titled `feat(<module>): <module-name> <platform-suffix>` (e.g. `feat(dashboard): dashboard backend`, `feat(dashboard): dashboard iOS`)
- Backend issue should add `type:contract` if a new contract fragment is needed

For **cross-cutting / flow specs** (group-training, vacation-mode, onboarding-wizard):
1. Epic issue in `project-spec` repo:
   - Title: `epic(<module>): <human readable feature>` (e.g. `epic(group-training): group training feature`)
   - Labels: `phase-4`, `epic`, `module:<module>`
   - Body: tracking checklist with placeholder links to sub-tasks (filled in after sub-tasks are created)
   - Assignee: leave empty (epics track, don't assign)
2. Sub-tasks in each platform repo it touches
   - Title: `feat(<module>): <module> <platform-suffix>`
   - Body line at the top: `Part of epic: 321-fit/project-spec#<epic-number>`
   - Labels: `phase-4`, `module:<module>`, `repo:<repo>` (no `epic` label on sub-tasks)
3. After all issues are created, **edit the epic body** to insert real sub-task links

For **reusable component specs** (sport-picker, location-picker, session-creation):
- Behave like module specs (1 issue per platform), but add `type:component` label if the design-tokens repo also needs a component issue
- If a design-tokens issue is needed, add it to the plan separately with `repo:design-tokens` + `type:component`

---

## Step 3 — Issue body template

Every issue body must be **self-contained** so a developer (human or AI agent) can pick it up and work autonomously without further questions. Required sections:

```markdown
## How to run (via OpenClaw in Mattermost)

Send to the bot in the issue thread, verbatim:

> запусти `/<architect-command> impl-doc <spec-name>`

Where `<architect-command>` is:
- `architect` for `repo:poly-backend`
- `ios-architect` for `repo:321fit_ios`
- `android-architect` for `repo:321fit_android_new`

The bot translates this into a Claude Code agent run with the spec name as argument. Do NOT ask the bot to "choose between impl-doc / spec / analyze" — `impl-doc` is the canonical Phase 4 command; the rest are not used for this flow.

## Cross-repo paths the agent needs

The spec, prototype, and contract live in the sibling `project-spec` repo (not the platform repo this issue lives in):

- Spec source: `project-spec/specs/<spec-name>.md` — fetch via [GitHub Pages](https://321-fit.github.io/project-spec/specs/<spec-name>.md) if no local checkout
- Prototype HTML: `project-spec/prototypes/flows/<role>/<flow>.html` — fetch via Pages URL if no local checkout
- Contract: `project-spec/contracts/<spec-name>.openapi.yaml` — produced by the backend issue in this batch
- Design system + native theming contract: `project-spec/architecture/design-system.md`
- Baseline OpenAPI: `project-spec/contracts/_baseline.openapi.yaml`

If only the platform repo is checked out, the GitHub Pages URLs above are authoritative — use `WebFetch` / `curl` to read them. Do not stop because local fs lacks `contracts/`, `specs/`, or `prototypes/`.

## If backend contract is missing — DO NOT STOP   *(client-platform issues only; omit from backend issue body)*

The contract `project-spec/contracts/<spec-name>.openapi.yaml` may not exist yet at the time you run impl-doc. **This is the expected state.** The backend issue in this batch generates it in parallel.

Per `MOCK_POLICY.md` + skill rules, the agent must proceed **mock-first**:

1. Generate `<repo>/docs/<spec-name>-<platform>.md` with a contract sketch in §6 marked `TBD: backend#<n> finalizes`
2. Build the mock provider so it returns full snapshots for every state from spec §5
3. Wire the `MOCK_<MODULE>` build flag (xcconfig on iOS, gradle.properties on Android) so the dev can run the screen against mocks without a backend
4. When the backend issue merges → swap the contract reference, flip the mock flag to false, re-test

Do NOT ask "impl-doc или spec?" — `impl-doc` is correct. The contract gap is handled by the mock layer, not by switching mode.

---

## Spec
- Spec: [project-spec/specs/<spec-name>.md](https://321-fit.github.io/project-spec/specs/<spec-name>.md) — sections relevant to this issue
- Prototype: [<flow file>#<screen-id>](https://321-fit.github.io/project-spec/prototypes/flows/<...>.html#<screen-id>)
- Contract (if backend): `project-spec/contracts/<spec-name>.openapi.yaml` — fragment to create / extend
- Impl-doc target: `<repo>/docs/<spec-name>-<platform>.md` (to be created by /architect impl-doc)

## Related specs (read these too — implementation depends on them)
<populate from the spec's bottom "Related specs / references" section. For each entry, copy the link AND add a one-line note saying *why* this spec matters for the current issue — e.g. "defines the 6 wizard steps and their semantics" or "defines sessionsToReview data source (uses training_event filtered by status='review')". Do NOT skip this — the dry run on dashboard.md proved that without it, /architect agents fabricate questions that are already answered in sibling specs.>

## Summary
<2-3 sentences: what this issue delivers from the spec, scoped to this platform>

## Components used (if UI)
- `FitSelectionGroup`, `FitSheet`, `FitButton`, ... — link to `design-tokens/docs/components.md` if relevant

## Endpoints touched (if backend or client)
- `GET /api/v1.0.0/coach/<resource>` — existing (extend) or new
- Reference baseline: `project-spec/contracts/_baseline.openapi.yaml`

## Implementation order
1. Run `/architect impl-doc <spec-name>` (in this repo) to generate impl-doc + contract fragment if needed
2. Run `/develop implement <spec-name>` — branches off the active integration branch (read from `.claude/CURRENT_INTEGRATION_BRANCH`)
3. Open PR into the integration branch with the test plan from `/develop`

## Acceptance criteria

> **Note on checkboxes.** The issue body is treated as a **static contract** — it lists what must be true at completion, not a working checklist. Reviewers tick the same items off in the **PR description's "Test plan" section**, which is the working checklist. When the PR merges with `Closes #<issue-number>`, the issue auto-closes regardless of body checkboxes. This avoids busy-work syncing two checklists.
>
> Mark a criterion `[x]` here only if the work was already done before the issue was created (e.g. impl-doc generated by `/architect impl-doc` immediately prior). Otherwise leave `[ ]` — the PR's Test plan tracks completion.

- [ ] Impl-doc exists at `<repo>/docs/<spec-name>-<platform>.md` and matches IMPL_DOC_TEMPLATE
- [ ] Contract fragment (if backend) exists and is reviewed
- [ ] All flows from spec section 4 implemented
- [ ] All states from spec section 5 covered
- [ ] All business rules from spec section 7 enforced
- [ ] Tests per spec section 9: happy + 401/403/404 (backend) or state transitions (UI)
- [ ] No drift between code and contract — `/architect review` passes
- [ ] PR description references this issue (`Closes #<issue-number>`) and mirrors these criteria in its Test plan

## Memory rules to apply
- `feedback_read_prototype_first` — read prototype HTML + annotations before asking any product question
- `feedback_keep_existing_endpoints` — extend existing paths, don't rename
- `feedback_native_pickers` — wheel/time/date/map = native, not Fit components
- `feedback_skills_language` — all code/docs/PR descriptions in English
- `feedback_openclaw_issue_body` — when bot asks "which mode" or stops on missing contract, point it at the How-to-run + If-contract-missing blocks at the top of this issue
- (Add module-specific memory references if they exist, e.g. `project_locations_decisions`)

## Dependencies
- Blocked by: <other issue(s)>
- Blocks: <other issue(s)>

## Open questions (if any from spec section 10)
- <quoted question, owner: product / architect / TBD>
```

For Epic issues (cross-cutting), the body is shorter — see Step 2.

---

## Step 4 — Show plan, wait for approval

Print the full plan as a table — one row per issue:

| # | Repo | Title | Labels | Assignees |
|---|---|---|---|---|

After the table, show the **first issue body in full** as a sample so the user can verify the format, then say:

> "I will create N issues in repos [...] and add them all to Project #2. The first issue body is shown above as a sample — the rest follow the same template, populated per platform.
>
> Reply 'go' to create, or describe edits."

**Do not create issues until the user replies "go" or equivalent.**

---

## Step 5 — Create issues (after approval)

Sequence:
1. **For epics** — create the epic issue first, save its number
2. Create each sub-task issue with the epic reference in body
3. Each issue:
   - `gh auth status` — verify `whywolfy` is active
   - `gh issue create --repo 321-fit/<repo> --title "..." --body "..." --label "phase-4,module:<x>,repo:<x>" --assignee <username>` (multi-assign for backend: repeat `--assignee`)
   - Capture issue URL from output
4. Add each created issue to Project #2:
   - `gh project item-add 2 --owner 321-fit --url <issue-url>`
5. **For epics** — `gh issue edit <epic-number> --body "..."` to insert real sub-task links into the tracking checklist

After all issues are created, print a summary:

```
Created N issues for spec <spec-name>:
  - 321-fit/poly-backend#<n>  feat(<module>): <module> backend
  - 321-fit/321fit_ios#<n>    feat(<module>): <module> iOS
  - ...
All added to Project #2.
```

---

## Safety nets

- **NEVER** create issues without explicit user approval at Step 4
- **NEVER** rename existing backend endpoints in issue bodies — extend in place per `feedback_keep_existing_endpoints`
- **NEVER** propose components that don't exist in design-tokens — verify against `design-tokens/docs/components.md` and `Sources/FitUI/Components/` listing
- If a referenced screen ID does not exist in the prototype, flag it instead of inventing — ask the user
- All issue bodies in **English** per `feedback_skills_language`

---

## Quick command reference

```bash
# Verify auth (always run first)
gh auth status | head -3 | grep whywolfy

# Create issue with full label set + assignee
gh issue create \
  --repo 321-fit/poly-backend \
  --title "feat(dashboard): dashboard backend" \
  --body "..." \
  --label "phase-4" --label "module:dashboard" --label "repo:poly-backend" \
  --assignee iQiexie --assignee Nikita-Savelev

# Add to Project #2
gh project item-add 2 --owner 321-fit --url https://github.com/321-fit/poly-backend/issues/52

# Edit issue body (used after epic sub-tasks exist)
gh issue edit 52 --repo 321-fit/project-spec --body-file /tmp/epic-body.md
```

---

## Examples per classification

### Module spec — `dashboard`

5 issues:
1. `repo:poly-backend` — `feat(dashboard): dashboard backend` (assigned: iQiexie + Nikita-Savelev)
2. `repo:321fit_ios` — `feat(dashboard): dashboard iOS` (assigned: evvseenko)
3. `repo:321fit_android_new` — `feat(dashboard): dashboard Android` (assigned: spbisya)

Each gets `phase-4` + `module:dashboard` + relevant `repo:*` label. If dashboard is the pilot, also add `pilot`.

### Cross-cutting spec — `group-training`

5+ issues:
1. `repo:project-spec` — `epic(group-training): group training feature` (no assignee, body = tracking checklist)
2. `repo:poly-backend` — `feat(group-training): group training backend` (`Part of epic: project-spec#X` in body)
3. `repo:321fit_ios` — `feat(group-training): group training iOS`
4. `repo:321fit_android_new` — `feat(group-training): group training Android`
5. (optional) `repo:design-tokens` — additional `feat(components): <component>` if a new shared component is needed for this feature

### Reusable component spec — `sport-picker`

3 issues (or 4 if a design-tokens component is needed):
1. `repo:poly-backend` — endpoint additions (taxonomy listing if going server-side)
2. `repo:321fit_ios` — sport picker screen
3. `repo:321fit_android_new` — sport picker screen
- (`repo:design-tokens` only if a new Fit-prefixed component lands here, e.g. FitSportCard if extracted)
